{
  "name": "@starships/styles",
  "version": "2.1.0",
  "private": true,
  "devDependencies": {
    "@starships/webpack": "workspace:*",
    "@starships/module-federation-config": "workspace:*",
    "@starships/tsconfig": "workspace:*"
  },
  "scripts": {
    "dev": "webpack-cli serve --mode development",
    "build": "webpack --mode production",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  }
}
